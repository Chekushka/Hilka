export default function Home() {
  return (
    <main className="mx-auto flex max-w-2xl flex-1 flex-col justify-center p-6">
      <h1 className="text-2xl font-semibold text-ink">Hilka</h1>
      <p className="mt-2 text-ink-muted">
        The student workspace goes here. Until then, the runner has a development
        surface at <code className="font-mono text-sm">/runner</code>.
      </p>
    </main>
  );
}
